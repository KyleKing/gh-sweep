See screenshots on desktop:

1. I want to 'open' the repo and make edits
1. Remove excess metadata (fork is useful, but star/follower not)
1. Are the categories right? There seems to be overlap
1. Unwatched is misleading because it is the default (I just prefer to have All activity)
1. Custom isn't supported because I used to have it for Corallium with releases turned off, but that appeared as default

2026-07-26 decisions needed:

- Tags v0.1.0-v0.3.2 have no GitHub releases because goreleaser first succeeded at v0.3.3: backfill or ignore? (resolved 2026-07-27: deleted)
- Adopt .copier-answers.yml so this repo becomes a real copier child? (resolved 2026-07-27: adopted at my_go_template v0.6.1)

2026-07-27:

- Formula/gh-sweep.rb still pins version "0.1.0" with REPLACE_WITH_SHA256 placeholders, so its download URLs 404. As of v0.4.4 the release carries four real, distinct binaries for the platforms the formula covers, so it can now be filled in for real: set version "0.4.4" and paste the darwin-arm64, darwin-amd64, linux-arm64, and linux-amd64 sums from the release's checksums.txt. Its `test do` block asserts the version string, which the v0.4.4 binary prints. The standing question is whether to hand-maintain it at all: scripts/provision-tap-deploy-key.sh already exists for a goreleaser-pushed tap, and .goreleaser.yml has no brews block, so adding one would keep the formula current on every release instead of going stale after each one

2026-07-27 back-ports for my_go_template: all seven landed in template v0.6.3 and arrived here via copier update

2026-07-27 remaining back-ports for my_go_template (another agent owns that checkout; route these rather than editing it here). Re-checked against template v0.7.0 on 2026-07-30: all three are still open, since v0.7.0 touched only .goreleaser.yml, .pre-commit-config.yaml, .cz.toml, .golangci.toml, bump_version.yml, template.toml, and the tap-provision script, never CONTRIBUTING.md.jinja:

- CONTRIBUTING.md renders "gh gh-sweep ..." for the extension invocation, but a `gh-<name>` extension is invoked as `gh <name>`; the template should strip the `gh-` prefix in that sentence
- CONTRIBUTING.md dropped the `mise dev -- [args]` alternative next to `go run ./cmd/...`, though conf.d/template.toml still defines the `dev` task
- CONTRIBUTING.md troubleshooting could carry the golden-refresh command (`go test -tags=golden -run TestGolden ./... -update`) behind the same flag that gates the golden test task; re-applied by hand here

2026-07-30:

- Formula/gh-sweep.rb is now redundant. Template v0.7.0 added an unconditional `homebrew_casks` block to .goreleaser.yml, so every release generates and (once TAP_DEPLOY_KEY exists) pushes a cask to KyleKing/homebrew-tap. The hand-written stub still pins version "0.1.0" with REPLACE_WITH_SHA256 placeholders, so its download URLs 404 and its `test do` block would fail. The `brew:sha` mise task exists only to fill those placeholders in by hand. my_go_template's NEXT_STEPS.md says the same for the template's own stub and its release:homebrew task, and gates removal on a `remove-if-found.txt` manifest. Not deleted here: the removal should come from the template so every child loses it in one update, and CONTRIBUTING.md's "Updating the Homebrew Formula" section goes with it
- TAP_DEPLOY_KEY is not provisioned on this repo, so releases publish binaries and no cask. Verified locally that this is a warning and not a failure: `goreleaser release --snapshot --skip=publish` with the variable unset built all 10 targets, wrote dist/homebrew/Casks/gh-sweep.rb, and exited 0. Run scripts/provision-tap-deploy-key.sh when the tap should start receiving casks
