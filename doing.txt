See screenshots on desktop:

1. I want to 'open' the repo and make edits
1. Remove excess metadata (fork is useful, but star/follower not)
1. Are the categories right? There seems to be overlap
1. Unwatched is misleading because it is the default (I just prefer to have All activity)
1. Custom isn't supported because I used to have it for Corallium with releases turned off, but that appeared as default

2026-07-26 decisions needed:

- Tags v0.1.0-v0.3.2 have no GitHub releases because goreleaser first succeeded at v0.3.3: backfill or ignore? (resolved 2026-07-27: deleted)
- Adopt .copier-answers.yml so this repo becomes a real copier child? (resolved 2026-07-27: adopted at my_go_template v0.6.1)

2026-07-27:

- Formula/gh-sweep.rb still pins version "0.1.0" with REPLACE_WITH_SHA256 placeholders, so its download URLs 404; update it to 0.4.2 with real sums from `mise run brew:sha` or drop the formula

2026-07-27 back-ports for my_go_template (another agent owns that checkout; route these rather than editing it here):

- bump_version.yml should run goreleaser inline instead of softprops/action-gh-release. Tags pushed with GITHUB_TOKEN do not trigger release.yml, so template children get a release with notes and no binaries. gh-sweep's version is the working one
- release.yml is unreachable for bot-pushed tags; either drop it or document it as the manual-tag fallback only
- .goreleaser.yml uses two keys goreleaser v2 deprecated: `format: binary` should be `formats: [binary]`, and `snapshot.name_template` should be `snapshot.version_template`
- the child render of .pre-commit-config.yaml installs the copier-template-tester hook; children are not templates and have nothing for ctt to test
- hk.pkl dropped check-yaml and check-vcs-permalinks when it moved to 1.53.0 builtins, with no replacement
- hk.pkl needs a global `exclude` (or per-step exclude) for byte-exact snapshot files; the newlines and trailing-whitespace builtins rewrite .golden fixtures and break golden tests
- release.yml pins checkout and setup-go a major behind what gh-sweep carries (checkout v7.0.1, setup-go v7.0.0)
